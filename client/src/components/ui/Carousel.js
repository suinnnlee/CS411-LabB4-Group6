const Carousel = ({name, content}) => {
    return (
        <div class="container">
            <div class="slider">
                {content}
            </div>
        </div>
    )
}
export default Carousel